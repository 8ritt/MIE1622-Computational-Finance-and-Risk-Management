function  [x_optimal cash_optimal, borrow] = strat_buy_and_hold(x_init, cash_init, mu, Q, cur_prices, rf, borrow_init)

   x_optimal = x_init;
   cash_optimal = cash_init;
   borrow = borrow_init;
end